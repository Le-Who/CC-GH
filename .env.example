# Discord Application Credentials (from https://discord.com/developers/applications)
DISCORD_CLIENT_ID=your_client_id_here
DISCORD_CLIENT_SECRET=your_client_secret_here

# Must match "Redirects" in Discord Developer Portal > OAuth2
DISCORD_REDIRECT_URI=https://your-cloud-run-url.a.run.app

# Server port (Cloud Run sends PORT=8080 automatically)
PORT=8090

# Google Cloud Storage bucket for persistent data
# Leave empty for local-only mode (data lost on container restart)
GCS_BUCKET=

# Custom domain for non-Discord access (optional)
# When set, CORS and CSP will include this domain
# Example: gamehub.example.com
CUSTOM_DOMAIN=

# Note: Simple Auth (username/password login) is always enabled.
# When Discord credentials are configured, Discord OAuth2 is used inside Discord.
# Outside Discord (e.g. via custom domain or hosting URL), Simple Auth is used.
# If no auth is configured at all, the app runs in demo mode.
